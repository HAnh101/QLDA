//Viết chương trình nhập vào một chuỗi và đếm số 
//lần xuất hiện của mỗi ký tự trong chuỗi đó bằng cách sử dụng con trỏ.

#include<iostream>
using namespace std;

int main(){
    char a[100];
    cin >> a;
    int count[256];
    int b = int(a);
   
}